#include "main.h"

/**
 * _islower - check the lowercase character
 *at c: the character to check
 * Return: Always 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
